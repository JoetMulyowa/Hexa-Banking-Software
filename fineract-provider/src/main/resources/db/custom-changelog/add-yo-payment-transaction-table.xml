<?xml version="1.0" encoding="UTF-8"?>
<!--

    Licensed to the Apache Software Foundation (ASF) under one
    or more contributor license agreements. See the NOTICE file
    distributed with this work for additional information
    regarding copyright ownership. The ASF licenses this file
    to you under the Apache License, Version 2.0 (the
    "License"); you may not use this file except in compliance
    with the License. You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing,
    software distributed under the License is distributed on an
    "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
    KIND, either express or implied. See the License for the
    specific language governing permissions and limitations
    under the License.

-->
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.1.xsd">

    <!-- First check if the table exists -->
    <changeSet id="check-yo-payment-transaction-table" author="fineract">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="m_yo_payment_transaction"/>
            </not>
        </preConditions>
        <comment>Create YO Payment transaction table if it doesn't exist</comment>
        <sql>
            -- Create a dedicated table for Yo Payment transactions
            CREATE TABLE IF NOT EXISTS `m_yo_payment_transaction` (
              `id` BIGINT NOT NULL AUTO_INCREMENT,
              `loan_id` BIGINT NOT NULL,
              `loan_transaction_id` BIGINT NULL,
              `vendor_tran_id` VARCHAR(100) NULL,
              `middleware_reference_no` VARCHAR(100) NULL,
              `transaction_date` DATE NOT NULL,
              `amount` DECIMAL(19,6) NOT NULL,
              `is_reversed` TINYINT NOT NULL DEFAULT 0,
              `status_code` VARCHAR(10) NULL,
              `status_desc` VARCHAR(100) NULL,
              `request_body` TEXT NULL,
              `response_body` TEXT NULL,
              `error_msg` VARCHAR(500) NULL,
              `createdby_id` BIGINT NOT NULL,
              `created_date` DATETIME NOT NULL,
              `lastmodifiedby_id` BIGINT NULL,
              `lastmodified_date` DATETIME NULL,
              PRIMARY KEY (`id`),
              CONSTRAINT `FK_yo_payment_transaction_loan` FOREIGN KEY (`loan_id`) REFERENCES `m_loan` (`id`),
              CONSTRAINT `FK_yo_payment_transaction_loan_transaction` FOREIGN KEY (`loan_transaction_id`) REFERENCES `m_loan_transaction` (`id`)
            )
            ENGINE=InnoDB
            DEFAULT CHARSET=utf8mb4
            COLLATE=utf8mb4_unicode_ci;
        </sql>
    </changeSet>

    <!-- Add middleware_reference index if it doesn't exist -->
    <changeSet id="add-yo-payment-transaction-middleware-index" author="fineract" runOnChange="true">
        <preConditions onFail="MARK_RAN">
            <not>
                <indexExists indexName="IDX_yo_payment_transaction_middleware_reference" tableName="m_yo_payment_transaction"/>
            </not>
        </preConditions>
        <comment>Create middleware reference index if it doesn't exist</comment>
        <sql>
            CREATE INDEX `IDX_yo_payment_transaction_middleware_reference` ON `m_yo_payment_transaction` (`middleware_reference_no`);
        </sql>
    </changeSet>

    <!-- Add vendor_tran_id index if it doesn't exist -->
    <changeSet id="add-yo-payment-transaction-vendor-index" author="fineract" runOnChange="true">
        <preConditions onFail="MARK_RAN">
            <not>
                <indexExists indexName="IDX_yo_payment_transaction_vendor_tran_id" tableName="m_yo_payment_transaction"/>
            </not>
        </preConditions>
        <comment>Create vendor transaction ID index if it doesn't exist</comment>
        <sql>
            CREATE INDEX `IDX_yo_payment_transaction_vendor_tran_id` ON `m_yo_payment_transaction` (`vendor_tran_id`);
        </sql>
    </changeSet>

    <!-- Add vendor_tran_id column if it doesn't exist -->
    <changeSet id="add-vendor-tran-id-column" author="fineract" runOnChange="true">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="m_yo_payment_transaction" columnName="vendor_tran_id"/>
            </not>
        </preConditions>
        <comment>Add vendor_tran_id column if it doesn't exist</comment>
        <sql>
            ALTER TABLE `m_yo_payment_transaction` ADD COLUMN `vendor_tran_id` VARCHAR(100) NULL;
        </sql>
    </changeSet>
</databaseChangeLog>
